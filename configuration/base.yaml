# NOTE: not to be imported in production projects

# Separate settings will be used for local and docker containerized environments
application:
  port: 9001
  # This needs to be set as `APP_APPLICATION_HMAC_SECRET` environment variable on Cloud Service provider (Digital Ocean) for production
  hmac_secret: "super-long-and-secret-random-key-needed-to-verify-message-integrity"

database:
  host: "127.0.0.1"
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "newsletter"

# using the default host and port for redis configuration
redis_uri: "redis://127.0.0.1:6379"